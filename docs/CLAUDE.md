# Mihan — Claude Code Project Context

The canonical, maintained project context lives at the repo root: [`../CLAUDE.md`](../CLAUDE.md).

This file used to hold a full copy, which drifted out of date (it was missing
the live Wathq integration, Docker setup, test suite, and CI). To avoid two
sources of truth, keep all project context in the root file only.
