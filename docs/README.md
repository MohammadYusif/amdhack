# Documentation has moved

The full project README now lives at the repository root — [`../README.md`](../README.md) — so GitHub renders it on the repo homepage.

This folder keeps:

- [`CLAUDE.md`](CLAUDE.md) — Claude Code project context (architecture, regulatory constraints, conventions)
- [`assets/`](assets/) — brand assets (Mihan logo)
